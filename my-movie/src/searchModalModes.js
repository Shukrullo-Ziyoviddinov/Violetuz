/** Search modal UI rejimlari — server/algoritmga bog‘liq emas.
 * browse  → Kino|Music (lupa ochilishi)
 * compose → History / yozish (input focus)
 * query   → Results (SearchModalBody)
 */
export const SEARCH_MODE_BROWSE = 'browse';
export const SEARCH_MODE_COMPOSE = 'compose';
