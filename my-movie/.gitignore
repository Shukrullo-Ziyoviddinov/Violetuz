# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# env / secrets — never commit
.env
.env.*
!.env.example

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Large catalog media: keep in public/ until Variant D migrate to R2 succeeds.
# Only ignore AFTER migrate+CDN rewrite, then prune. Do not ignore while using /img|/video paths.
# /public/video/
# /public/music/*.mp3
